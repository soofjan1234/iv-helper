## 文件索引同步的作用是什么

同步文件和数据库里的文件索引

本来是在网盘项目里实现这个功能，然后其它项目调用它，但是像相册、音乐都有自己需要的一些文件特性，比如exif、id3等，会使得网盘存储一些不需要的属性

所以加个中间层，文件索引同步器对文件进行同步，网盘、相册、音乐来使用文件索引同步器更新数据库，做到了单一职责以及解耦

## 为什么不用fsnotify

1. **不递归**：新建子目录要手动加 watcher
2. **内存问题**：目录多了 watcher 爆炸
3. **事件不可靠**：重命名会丢事件，批量操作会重复

 所以我直接换了方案，用 **主动轮询 + SQLite 状态机**，放弃实时性换取可靠性。

## 海量文件场景下，fsnotify 的内存开销主要来自哪里？你们的方案是如何降低 90% 的？

| 开销来源 | 说明 |
| --- | --- |
| **inotify watch 实例** | 每个被监控的目录都需要占用一个 watch 句柄，Linux 默认限制 524288 个 |
| **内核事件队列** | 每个文件变更都会产生事件，排队等待用户空间读取 |
| **路径解析缓存** | 收到事件后需要解析完整路径，内核/用户空间都需要缓存 |
| **活动监控开销** | 即使文件没变化，inotify 也需要维护监控状态 |

核心思路：不监听，只按需扫描
fsnotify 模式:  监听 /data 整个目录树  → 假设有 100 万个文件  → 需要 100 万个 watch  → 内存持续占用
album-filesyncer 模式:  只在需要时扫描特定目录  → 假设每次只扫描 1000 个目录  → 内存占用 = 1000 个目录信息  → 扫描完就释放

## 为什么是BFS而不是DFS

1. **用户体验**：BFS 先同步浅层目录，用户常用的文件夹能更快被索引到
2. **代码简洁：**Go 的 channel 本身就是 FIFO 队列，天然适合 BFS，代码也更简洁

## "接口抽象"具体是怎么做的？定义了哪些接口？为什么这样划分？

有些接口只做定义，让用户自己实现，比如日志接口，适配不同日志框架；Model接口，定义自己的dao层方法

## What and Why is Functional Options

一种用**函数作为参数**来配置对象的设计模式

相比直接传参或传 Config 结构体，Functional Options

1. **可选参数**：调用方只传需要的，其他自动用默认值
2. **向后兼容**：新增配置项不破坏已有调用
3. **可校验**：在 Option 函数内可以做参数校验和默认值处理
4. **高度自解释**： WithPort(80) 比直接传个 80 清楚得多

## "跨项目的高内聚集成"具体指什么？如何实现的？
"高内聚"体现在
单一职责包：只有一个 FileSyncer 核心 struct，逻辑内聚
依赖注入：通过 Option 函数注入具体实现
零外部依赖：核心包只依赖 go.mod 中的基础库

"跨项目集成"指
这个 file-syncer 模块被独立发布为一个 Go Module，可以被其他项目（如 LincAlbum、LincWebdav 等）通过 import 集成

// ----问题----
1. 文件索引同步器的作用是什么
2. 为什么不用 fsnotify
3. 海量文件场景下 fsnotify 的内存开销主要来自哪里
4. 为什么是 BFS 而不是 DFS
5. 接口抽象具体是怎么做的，定义了哪些接口
6. Functional Options 是什么
7. 跨项目的高内聚集成具体指什么

// ---文件索引同步器---
|| 序号 | 上次考试日期 | 上次考试分数 | 下次考试日期 |
|| --- | --- | --- | --- |
|| 1 |
|| 2 |
|| 3 |
|| 4 |
|| 5 |
|| 6 |
|| 7 |